import ClientWrapper from "@/components/ui/ClientWrapper";

export default function Discover() {
  return (
    <main className="min-h-screen" style={{ background: 'var(--surface-primary)', color: 'var(--content-primary)' }}>
      <ClientWrapper />
    </main>
  );
}